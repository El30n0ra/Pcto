#include <stdio.h>

// trovare il tipo di triangolo (scaleno, isoscele, equilatero) in base alle misure dei lati

int main()
{
    int uno; 
    int due; 
    int tre;
    printf("fornire lunghezza lato uno: ");
    scanf("%i")
}